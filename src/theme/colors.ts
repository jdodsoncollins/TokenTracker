/**
 * Back-compat re-export. Prefer importing from `./tokens` for new code.
 */
export {
  colors,
  getTheme,
  spacing,
  radius,
  typography,
  isAppleChrome,
  isMaterialChrome,
  visualPlatform,
} from './tokens';
