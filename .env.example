# TokenTracker does not use environment variables for API keys.
# Add keys in the app UI — they are stored only on your device via
# the OS secure enclave (Keychain / Keystore).
#
# This file exists only so contributors know not to invent a secrets file.
