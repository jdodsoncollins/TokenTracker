# TokenTracker does not use environment variables for API keys.
# Native apps persist keys in OS secure storage through expo-secure-store.
# Web builds keep keys in memory and discard them on reload.
# TokenTracker does not require biometric authentication or claim that keys
# use secure enclave hardware.
#
# This file exists only so contributors know not to invent a secrets file.
